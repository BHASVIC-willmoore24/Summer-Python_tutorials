pip packages stored at pypi.org  
packages installed with `pip install <package>`  
installing packages this way sytemwide can break system packages and is not recommended so usually installed in a venv  
